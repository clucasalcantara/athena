export const types = `
  type insertResponse {
        generated_id: String
        success: Boolean
    }

    type removeResponse {
        removed_id: String
        success: Boolean
    }

    type updateResponse {
        updated_id: String
        success: Boolean
    }
`
export const queries = ``

export const mutations = ``
